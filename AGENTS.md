# プロジェクトガイドライン

## プロジェクト構成

- このリポジトリには、`fan_speed.5s.js` に CommonJS 形式の BitBar/SwiftBar プラグインが 1 つあります。
- パース処理は小さな export 済みヘルパーに分け、外部コマンドの実行と表示との境界は `run()` が担当します。
- インストール方法と macOS/iSMC のセットアップについては、既存の README を参照してください: [README.md](README.md)。

## コードスタイル

- CommonJS、プロダクションコードでは 4 スペースのインデント、シングルクォート、セミコロンを使用します。
- 既存の防御的なパース動作を維持します。不正な入力では `null` を返し、有限でない数値は無視します。
- プラグインの標準出力は BitBar/SwiftBar 互換にします。出力仕様を意図的に変更する場合を除き、`SWIFTBAR=1` によるアイコンとオプションの違いを維持してください。

## ビルドとテスト

- ビルド手順や npm script は定義されていません。
- 有効な自動テストは `node --test __tests__/fan_speed.node.test.js` で実行します。
- プラグインは `node fan_speed.5s.js` で手動実行できます。macOS、`/opt/homebrew/bin/ismc`、および `PATH` 上の `jq` が必要です。
- `__tests__/fan_speed.test.js` は未完成の Jest 用スキャフォールドで、Jest は宣言されていません。有効なカバレッジとして扱わず、標準のテストコマンドにも指定しないでください。

## 変更時の指針

- `extractBytesPayload()` または `parseFanData()` を変更する場合は、対象を絞った Node テストを追加または更新します。
- コマンド実行、標準エラー出力の診断メッセージ、最終的な標準出力行は、公開された統合境界として扱います。
- 対応ランタイムに関する README とテストも更新せずに、ハードコードされた iSMC のパスやシェルパイプラインを置き換えないでください。